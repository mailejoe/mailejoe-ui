export interface Translation {
  required?: string,
  invalidEmail?: string,
  forgotPassword?: string,
  password?: string,
  email?: string,
  login?: string;
  LanguagePicker: {
    header?: string;
  };
  ErrorPage: {
    title?: string;
    description?: string;
  };
}
